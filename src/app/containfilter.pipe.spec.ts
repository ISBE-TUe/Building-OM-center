import { ContainfilterPipe } from './containfilter.pipe';

describe('ContainfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContainfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
