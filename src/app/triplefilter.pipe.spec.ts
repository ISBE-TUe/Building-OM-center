import { TriplefilterPipe } from './triplefilter.pipe';

describe('TriplefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TriplefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
